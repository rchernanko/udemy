package interfaces;

public interface Info {
    //Note that this is not a class, but an interface. Icons look different in the Project left panel too

    public void showInfo();
    //In interfaces, you never define any code in the body of the method. You only ever specify the header

}
